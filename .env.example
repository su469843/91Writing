# 墨章 后端环境变量配置
# 复制此文件为 .env 并填入实际值

# JWT 签名密钥（必填！生产环境必须使用强随机字符串）
# 生成方式：openssl rand -base64 32
JWT_SECRET=your-strong-random-secret-here

# API Key 加密密钥（可选，如不设置则使用 JWT_SECRET 派生）
# 生成方式：openssl rand -hex 32
ENCRYPTION_KEY=
