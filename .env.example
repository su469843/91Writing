# 墨章 后端环境变量配置
# ⚠️ 重要：复制此文件为 .env 并在生产环境前填入实际值！
#    如果 JWT_SECRET 和 ENCRYPTION_KEY 使用默认值，攻击者可伪造用户身份！

# JWT 签名密钥（必填！生产环境必须使用强随机字符串）
# 生成方式：openssl rand -base64 32
JWT_SECRET=your-strong-random-secret-here

# API Key 加密密钥（强烈建议单独设置，不要与 JWT_SECRET 相同）
# 生成方式：openssl rand -hex 32
# 如果不设置，将回退使用 JWT_SECRET 派生，安全性降低
ENCRYPTION_KEY=
